# 安裝設定說明書

這份說明會帶你把「會議室預約系統」架設起來。

**你不需要懂程式，也不需要安裝任何軟體。** 全部都在網頁上完成，用手機也可以。

- 需要的時間：大約 30 到 40 分鐘
- 需要的費用：**新台幣 0 元**（全部使用永久免費方案，不需要信用卡）
- 需要準備：一個 GitHub 帳號、一個可以收信的電子郵件信箱

> **小提醒**：這份說明會請你複製好幾組「很長的英文數字」。
> 建議你先開一個記事本，把它們暫時貼在裡面，最後再一次貼到 GitHub。
> 全部設定完成後，請把那個記事本刪掉。

---

## 目錄

1. [步驟一：建立資料庫（Neon）](#步驟一建立資料庫neon)
2. [步驟二：設定寄信服務（Brevo）](#步驟二設定寄信服務brevo)
3. [步驟三：建立網站主機（Render）](#步驟三建立網站主機render)
4. [步驟四：把密碼貼到 GitHub](#步驟四把密碼貼到-github)
5. [步驟五：按下部署按鈕](#步驟五按下部署按鈕)
6. [步驟六：第一次登入](#步驟六第一次登入)
7. [步驟七：開啟自動提醒信](#步驟七開啟自動提醒信)
8. [常見問題](#常見問題)

---

## 步驟一：建立資料庫（Neon）

資料庫是存放所有預約紀錄的地方。我們使用 Neon 的免費方案，**資料不會過期，也不需要信用卡**。

1. 打開 <https://neon.com> ，按右上角的 **Sign up**。
2. 選擇 **Continue with GitHub**，用你的 GitHub 帳號登入即可。
3. 登入後它會請你建立一個專案（Project）。
   - Project name（專案名稱）：輸入 `booking`
   - 其他選項保持預設就好，按 **Create project**。
4. 建立完成後，畫面上會出現一段以 `postgresql://` 開頭的長字串，
   通常標示為 **Connection string**。
5. 按旁邊的**複製**按鈕，把它貼到你的記事本，並註明這是 **`DATABASE_URL`**。

> 這串文字看起來像這樣（你的會不一樣）：
> `postgresql://neondb_owner:xxxxxxxx@ep-xxxx-xxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require`

---

## 步驟二：設定寄信服務（Brevo）

系統需要寄出驗證信、預約確認信、提醒信。我們使用 Brevo 的免費方案，**每天可以寄 300 封**。

1. 打開 <https://www.brevo.com> ，按 **Sign up free** 註冊。
2. 註冊後到你的信箱收信，點擊 Brevo 寄來的驗證連結，完成信箱驗證。
3. **設定寄件人地址**：
   - 在左側選單找到 **Senders, Domains & Dedicated IPs**（或 **Senders**）。
   - 按 **Add a sender**，填入你要用來寄信的名稱與電子郵件地址
     （例如公司的行政信箱）。
   - Brevo 會寄一封確認信到那個地址，點擊裡面的連結完成確認。
   - 把這個電子郵件地址記到記事本，註明這是 **`EMAIL_FROM_ADDRESS`**。
4. **取得 API 金鑰**：
   - 點右上角的頭像 → **SMTP & API**（或左側選單的 **SMTP & API**）。
   - 切換到 **API Keys** 分頁，按 **Generate a new API key**。
   - 名稱隨意輸入（例如 `booking`），按確定。
   - 畫面會顯示一串以 `xkeysib-` 開頭的文字，**這串文字只會出現這一次**。
   - 立刻複製並貼到記事本，註明這是 **`EMAIL_PROVIDER_API_KEY`**。

---

## 步驟三：建立網站主機（Render）

Render 負責實際執行這個系統，並提供一個免費網址。

1. 打開 <https://render.com> ，按 **Get Started**，選擇用 GitHub 帳號登入。
2. 登入後按 **New +** → **Web Service**。
3. 選擇 **Build and deploy from a Git repository**，
   然後選擇這個系統所在的儲存庫（repository）。
   - 如果找不到，按 **Configure account** 授權 Render 讀取你的 GitHub。
4. 接著填寫設定：
   - **Name**（名稱）：例如 `meeting-room-booking`
     （這個名稱會變成你的網址，請記下來）
   - **Language / Runtime**：選 **Python 3**
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`gunicorn "app.web.app:build_wsgi_app()" --bind 0.0.0.0:$PORT`
   - **Instance Type**：選 **Free**（免費）
5. 往下找到 **Auto-Deploy**，把它設為 **No**。
   （我們要用 GitHub 上的按鈕來部署，而不是每次改程式就自動部署。）
6. 按 **Create Web Service**。
   - 第一次它會嘗試啟動並且**失敗**，這是正常的，因為還沒有設定下一步的環境變數。
7. **記下你的網址**：頁面最上方會顯示類似
   `https://meeting-room-booking.onrender.com` 的網址。
   複製到記事本，註明這是 **`APP_BASE_URL`**。
8. **在 Render 裡設定環境變數**：
   - 左側選單點 **Environment**，按 **Add Environment Variable**。
   - 依照下一節的表格，把 **8 個項目**逐一加入（名稱要一字不差）。
   - 全部加完後按 **Save Changes**。
9. **取得部署金鑰**：
   - 左側選單點 **Settings**，往下找到 **Deploy Hook**。
   - 複製那串網址，貼到記事本，註明這是 **`RENDER_DEPLOY_HOOK_URL`**。

---

## 步驟四：把密碼貼到 GitHub

現在把記事本裡的所有項目貼進 GitHub。

### 需要的項目一覽

| 名稱（請一字不差地輸入） | 內容 | 從哪裡來 |
|---|---|---|
| `DATABASE_URL` | 資料庫連線字串 | 步驟一 |
| `EMAIL_PROVIDER_API_KEY` | 寄信服務的 API 金鑰 | 步驟二 |
| `EMAIL_FROM_ADDRESS` | 系統寄信時顯示的寄件人信箱 | 步驟二 |
| `APP_BASE_URL` | 你的網站網址 | 步驟三 |
| `SESSION_SECRET` | 保護登入狀態用的隨機字串 | 見下方說明 |
| `ADMIN_EMAIL` | 你（管理員）的電子郵件 | 自己決定 |
| `ADMIN_INITIAL_PASSWORD` | 管理員的第一次登入密碼 | 自己決定 |
| `CRON_SECRET` | 保護提醒信排程用的隨機字串 | 見下方說明 |
| `RENDER_DEPLOY_HOOK_URL` | Render 的部署金鑰網址 | 步驟三 |

**關於 `SESSION_SECRET` 和 `CRON_SECRET`**：
這兩個只要是「夠長又難猜」的隨機文字就好，不需要記住它們。
你可以隨手在鍵盤上打 40 個以上的英文字母和數字，例如：
`k8Fq2mZp7Lw4Rt9YvXb3Nc6Hd1Js5Gk0Aq2Wf8Ee`
兩個請使用**不一樣**的內容。

**關於 `ADMIN_INITIAL_PASSWORD`**：
這是你第一次登入用的密碼，至少 8 個字。
**系統會在你第一次登入後立刻要求你更換密碼**，所以這組密碼只會用一次。

### 貼上的步驟

1. 在 GitHub 上打開這個系統的儲存庫頁面。
2. 點上方的 **Settings**（設定）。
   - 如果沒看到，請把畫面往右滑，或點 **⋯** 展開更多選項。
3. 左側選單找到 **Secrets and variables**，點開後選 **Actions**。
4. 按綠色的 **New repository secret**。
5. **Name** 欄位輸入名稱（例如 `DATABASE_URL`），
   **Secret** 欄位貼上對應的內容，按 **Add secret**。
6. 重複第 4、5 步，直到上面表格中的 **9 個項目**全部加完。

> **名稱一定要完全一樣**，包含大小寫和底線。
> 如果打錯了，部署時會直接告訴你少了哪一個，回來補上即可。

**別忘了**：步驟三第 8 點提到的 Render 環境變數，
需要的是同一張表格裡的**前 8 個**項目
（不含 `RENDER_DEPLOY_HOOK_URL`，那個只有 GitHub 需要）。
兩邊都要設定，內容完全一樣。

---

## 步驟五：按下部署按鈕

1. 在 GitHub 儲存庫頁面，點上方的 **Actions**。
2. 左側清單點 **Deploy**。
3. 右側會出現 **Run workflow** 按鈕，點它。
4. 再點一次出現的綠色 **Run workflow** 確認。
5. 等待。畫面上會出現一列正在執行的工作，點進去可以看到進度。
   整個過程大約 3 到 8 分鐘。
6. 完成後：
   - **綠色勾勾 ✅** 代表成功。往下捲動可以看到「部署結果」，裡面有你的網站網址。
   - **紅色叉叉 ❌** 代表失敗。點開紅色的步驟就會看到原因，
     最常見的是少貼了某個 Secret，系統會直接把名稱列出來。
     補上之後再按一次 **Run workflow** 就好。

---

## 步驟六：第一次登入

1. 打開你的網站網址（`APP_BASE_URL`）。
   - **第一次開啟可能要等將近 1 分鐘**，這是免費方案的正常現象，請耐心等候。
2. 使用 `ADMIN_EMAIL` 和 `ADMIN_INITIAL_PASSWORD` 登入。
3. **系統會立刻要求你設定新密碼。** 請設定一組只有你知道的密碼。
4. 完成後你就進入管理後台了。建議接著做這幾件事：
   - **會議室**：修改或新增你們實際的會議室（系統已經預先建立了三間範例）。
   - **系統設定**：確認預約規則符合你們的需求
     （例如每次最長可預約幾小時、可以預約多久以後的時段）。
   - **邀請成員**：用「邀請」功能寄出邀請連結，
     收到的人點連結填完資料就能直接使用，不需要再審核。

---

## 步驟七：開啟自動提醒信

系統會在會議開始前寄提醒信給預約人，這件事由 GitHub 自動執行。

1. 在儲存庫頁面點 **Actions**。
2. 如果畫面出現「Workflows aren't being run on this repository」之類的提示，
   請按 **I understand my workflows, go ahead and enable them**。
3. 左側點 **Reminders**，確認它沒有被停用。

### ⚠️ 這件事請務必知道

GitHub 的自動排程有兩個限制：

1. **系統忙碌時，排程可能延遲幾分鐘才執行。** 這不影響正確性，提醒信仍然會寄出。
2. **如果這個儲存庫連續 60 天完全沒有任何動作，GitHub 會自動停用排程。**
   提醒信就會安靜地停掉，不會有人通知你。

**要怎麼發現？**
管理後台的首頁會顯示**「提醒信工作最後執行時間」**。
如果你看到那個時間停在很久以前，就表示排程被停用了。

**要怎麼恢復？**
回到 **Actions** → 左側點 **Reminders** →
如果看到 **Enable workflow** 按鈕就按下去。
按一次 **Run workflow** 可以立刻手動執行一次。

---

## 常見問題

**Q：網站打開很慢，轉圈圈很久。**
免費方案在沒有人使用 15 分鐘後會進入休眠，下一個人開啟時需要約 1 分鐘喚醒。
喚醒之後就會恢復正常速度。這是免費方案的正常現象，不是故障。

**Q：部署失敗，紅色叉叉。**
點開紅色的步驟看訊息。如果寫著「缺少 Secret：某某名稱」，
回到步驟四把它補上，注意名稱大小寫要完全一樣，然後重新執行一次。

**Q：收不到系統寄出的信。**
- 先看看垃圾郵件匣。
- 到管理後台的**寄信紀錄**頁面，看看那封信的狀態是「已寄出」還是「失敗」，
  失敗的話會顯示原因。
- 確認步驟二裡的寄件人地址已經完成 Brevo 的確認手續。

**Q：今天的信寄不出去了。**
免費方案每天上限 300 封，系統設定的安全上限是 280 封。
超過之後，系統會**優先停掉「會議提醒信」**，
但**驗證信、邀請信、密碼重設信、預約被取消的通知信仍然照常寄出**。
隔天就會自動恢復。

**Q：我想改預約規則（例如每次最長 2 小時）。**
管理後台 → **系統設定**，每一項旁邊都有中文說明。改完立即生效，不需要重新部署。

**Q：我要怎麼退回上一個版本？**
請看 [ROLLBACK.md](ROLLBACK.md)。

**Q：資料會不會不見？**
不會。Neon 的免費方案不會刪除資料。
另外，系統**永遠不會真正刪除預約紀錄**：
取消或被覆蓋的預約會保留成歷史紀錄，方便日後查詢。
