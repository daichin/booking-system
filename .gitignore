__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Claude Code 的工作目錄。git worktree 的簽出位置就在這裡面，
# 它本身不該被當成專案檔案提交進版本庫。
.claude/

# Local development database (SQLite backend only; production is Postgres)
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
/data/

.env
.env.*
!.env.example

.coverage
htmlcov/
.pytest_cache/

# 本機測試工具留下的東西。這個專案本身沒有用到 npm：瀏覽器測試需要的
# Playwright 是由 CI 用 pip 裝的（python -m playwright install），
# 這幾個檔案只是在本機跑測試時順手產生的，不屬於專案。
node_modules/
package.json
package-lock.json
