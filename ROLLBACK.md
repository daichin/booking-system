# 退回上一個版本

如果新版本出了問題，你可以把網站退回到之前正常運作的版本。

**全部都在 GitHub 網頁上完成，用手機也可以。**

---

## 先確認一件事

退回版本會還原**程式**，但**不會還原資料**。

也就是說：已經建立的預約、成員、會議室都會保留，不會消失。
這通常正是你想要的結果。

> ⚠️ 只有一個例外：如果新版本修改了資料庫的結構，
> 單純退回程式可能不夠。遇到這種情況，請把下面「找到出問題的版本」那一步
> 得到的資訊記下來，並尋求技術協助，不要重複嘗試部署。

---

## 步驟一：找到要退回的版本

1. 在 GitHub 打開這個系統的儲存庫。
2. 點檔案清單上方的 **Commits**（或時鐘圖示）。
3. 你會看到一份由新到舊的變更清單，每一列就是一個版本。
4. 找到**出問題之前**、最後一次正常運作的那一列。
5. 點該列右側的**複製圖示**，複製它的代碼（一串英數字，例如 `a1b2c3d`）。
   把它貼到記事本備用。

---

## 步驟二：把程式退回該版本

1. 回到儲存庫首頁，點上方的 **Code** 分頁。
2. 點左上角的分支選單（通常寫著 `main`）。
3. 在輸入框貼上剛才複製的代碼，選擇出現的那個版本。
4. 這時你正在檢視舊版本的內容。
   確認一下這確實是你要的版本。

### 用「還原」的方式（建議）

比起直接改動主分支，更安全的做法是**產生一個把變更倒回去的新版本**：

1. 回到 **Commits** 清單。
2. 點開**出問題的那一次**變更（不是正常的那一次）。
3. 在頁面右上角點 **⋯** → **Revert**（還原）。
   - 如果沒看到 Revert，代表這個版本不能自動還原，請改用下面的方式。
4. GitHub 會建立一個 Pull Request，按 **Create pull request**，
   再按 **Merge pull request** 完成合併。
5. 這樣主分支就回到了問題發生前的狀態，而且完整保留了紀錄。

---

## 步驟三：重新部署

1. 點上方的 **Actions**。
2. 左側點 **Deploy**。
3. 按 **Run workflow**。
4. 分支選 **main**，再按綠色的 **Run workflow**。
5. 等待 3 到 8 分鐘，看到綠色勾勾 ✅ 就完成了。
6. 打開網站確認問題已經解決
   （第一次開啟可能要等約 1 分鐘喚醒）。

---

## 如果部署一直失敗

1. 點開紅色的步驟，看最下面的錯誤訊息。
2. 如果寫著「缺少 Secret：某某名稱」，
   表示有設定被刪掉了，請依照 [SETUP.md](SETUP.md) 的步驟四重新加上。
3. 如果是「部署後健康檢查」這一步失敗，
   先直接打開網站看看能不能開。
   有時候只是免費方案還在喚醒，稍等一分鐘再執行一次部署即可。

---

## 緊急情況：先讓系統停止寄信

如果問題是「系統一直寄出錯誤的信」，你可以先止血，再慢慢處理：

1. 用管理員身分登入網站。
2. 進入管理後台 → **系統設定**。
3. 把 **`reminders_enabled`（啟用提醒信）** 改成關閉，儲存。

這會立刻停止提醒信，而且不需要重新部署。
