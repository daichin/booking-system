# 退回上一個版本

如果新版本出了問題，你可以把網站退回到之前正常運作的版本。

**全部都在 GitHub 網頁上完成，用手機也可以。**

---

## 先確認一件事

退回版本會還原**程式**，但**不會還原資料**。

也就是說：已經建立的預約、成員、會議室都會保留，不會消失。
這通常正是你想要的結果。

> ⚠️ 只有一個例外：如果新版本修改了資料庫的結構，
> 單純退回程式可能不夠。遇到這種情況，請把下面「找到出問題的版本」那一步
> 得到的資訊記下來，並尋求技術協助，不要重複嘗試部署。

---

## 步驟一：找到要退回的版本

1. 在 GitHub 打開這個系統的儲存庫。
2. 點檔案清單上方的 **Commits**（或時鐘圖示）。
3. 你會看到一份由新到舊的變更清單，每一列就是一個版本。
4. 找到**出問題之前**、最後一次正常運作的那一列。
5. 點該列右側的**複製圖示**，複製它的代碼（一串英數字，例如 `a1b2c3d`）。
   把它貼到記事本備用。

---

## 步驟二：把程式退回該版本

1. 回到儲存庫首頁，點上方的 **Code** 分頁。
2. 點左上角的分支選單（通常寫著 `main`）。
3. 在輸入框貼上剛才複製的代碼，選擇出現的那個版本。
4. 這時你正在檢視舊版本的內容。
   確認一下這確實是你要的版本。

### 用「還原」的方式（建議）

比起直接改動主分支，更安全的做法是**產生一個把變更倒回去的新版本**：

1. 回到 **Commits** 清單。
2. 點開**出問題的那一次**變更（不是正常的那一次）。
3. 在頁面右上角點 **⋯** → **Revert**（還原）。
   - 如果沒看到 Revert，代表這個版本不能自動還原，請改用下面的方式。
4. GitHub 會建立一個 Pull Request，按 **Create pull request**，
   再按 **Merge pull request** 完成合併。
5. 這樣主分支就回到了問題發生前的狀態，而且完整保留了紀錄。

---

## 步驟三：重新部署

1. 點上方的 **Actions**。
2. 左側點 **Deploy**。
3. 按 **Run workflow**。
4. 分支選 **main**，再按綠色的 **Run workflow**。
5. 等待 3 到 8 分鐘，看到綠色勾勾 ✅ 就完成了。
6. 打開網站確認問題已經解決
   （第一次開啟可能要等約 1 分鐘喚醒）。

---

## 如果部署一直失敗

1. 點開紅色的步驟，看最下面的錯誤訊息。
2. 如果寫著「缺少 Secret：某某名稱」，
   表示有設定被刪掉了，請依照 [SETUP.md](SETUP.md) 的步驟四重新加上。
3. 如果是「部署後健康檢查」這一步失敗，
   先直接打開網站看看能不能開。
   有時候只是免費方案還在喚醒，稍等一分鐘再執行一次部署即可。

---

## 緊急情況：先讓系統停止寄信

如果問題是「系統一直寄出錯誤的信」，你可以先止血，再慢慢處理：

1. 用管理員身分登入網站。
2. 進入管理後台 → **系統設定**。
3. 把 **`reminders_enabled`（啟用提醒信）** 改成關閉，儲存。

這會立刻停止提醒信，而且不需要重新部署。

---

# 清空資料（把系統清乾淨）

有時候你要的不是退回程式版本，而是**把資料清掉重來**：
試用期結束、換一個學期、或者範例資料已經沒有用了。

這件事同樣在 GitHub 網頁上完成，用手機也可以。

> ⚠️ **清掉的資料無法復原。**
> 系統不會保留備份，也沒有「上一步」可以按。
> 按下去之前請先確定你選對了範圍。

---

## 三種清除範圍

按 **Run workflow** 之前，要先在「清除範圍」選單挑一個。
下面由輕到重排列。

### `bookings`——只清預約

| 會被刪掉 | 會保留 |
|---|---|
| 所有預約（含已取消、被搶走的歷史紀錄） | 所有成員帳號 |
| 搶佔紀錄、寄信紀錄、稽核紀錄 | 所有會議室 |
| 登入嘗試紀錄、排程執行紀錄 | 所有系統設定 |

**大家不會被登出**，也不用重新設定任何東西，
已經寄出去的邀請信與密碼重設信仍然有效。
下次登入時看到的就是一個「沒有任何預約」的乾淨行事曆。

> 小提醒：管理後台的「提醒信工作最後執行時間」會暫時變成空白，
> 最多 15 分鐘後排程再跑一次就會自己恢復，不用理它。

### `members`——連同所有帳號一起清

| 會被刪掉 | 會保留 |
|---|---|
| **所有成員帳號，包含管理員** | 所有會議室 |
| 所有預約與所有紀錄（同上） | 所有系統設定 |
| 所有登入狀態、所有邀請與重設連結 | |

**所有人都會被登出，而且再也登不進來**，因為帳號已經不存在了。
清除完成後，系統會用 Secret `ADMIN_EMAIL` 與 `ADMIN_INITIAL_PASSWORD`
重新建立一個管理員帳號給你。

其他人必須重新註冊或由你重新邀請。

### `all`——全部清空，回到剛部署好的樣子

| 會被刪掉 | 會保留 |
|---|---|
| 上面兩種範圍的全部內容 | （資料庫的結構本身） |
| 所有會議室 | |
| 所有系統設定（改過的數值全部回到預設值） | |

清除完成後，系統會重新建立：

- 預設的系統設定（時段長度、預約上限、各等級配額等等，全部回到出廠值）
- 管理員帳號（同樣來自那兩個 Secret）
- 三間範例會議室

結果和「第一次部署完成」時完全一樣。

---

## 操作步驟

1. 在 GitHub 打開這個系統的儲存庫。
2. 點上方的 **Actions**。
3. 左側點 **Reset**。
4. 按右側的 **Run workflow**。
5. **清除範圍**：從選單選 `bookings`、`members` 或 `all`。
6. **確認文字**：輸入大寫的 `DELETE`。
   - 必須一字不差、全部大寫、前後不要有空白。
   - 輸入別的字（包括小寫的 `delete`）都會被擋下來，不會刪除任何資料。
7. 按綠色的 **Run workflow**。
8. 等待 1 到 3 分鐘，看到綠色勾勾 ✅ 就完成了。
9. 點進這次執行，最上面的摘要會列出**每一張資料表實際刪掉幾筆**。

---

## 清除之後要怎麼登入

### 如果你選的是 `bookings`

什麼都不用做，你原本就還登入著。

### 如果你選的是 `members` 或 `all`

你原本的管理員密碼已經連同帳號一起被刪掉了。
請改用**當初設定在 GitHub Secret 裡的那組**：

- 帳號：Secret `ADMIN_EMAIL` 的值
- 密碼：Secret `ADMIN_INITIAL_PASSWORD` 的值

登入後系統會**立刻要求你變更密碼**，這是正常的。

> 忘記那兩個 Secret 的值了？
> GitHub 不會讓你看到已存的 Secret 內容，但你可以直接覆蓋掉它：
> 到 **Settings → Secrets and variables → Actions**，
> 點 `ADMIN_INITIAL_PASSWORD` 旁邊的鉛筆圖示，填入一組新密碼並儲存，
> 然後**再執行一次 Reset**（範圍選 `members` 即可），新密碼就會生效。

---

## 如果清除失敗了

刪除本身是**一次寫入完成**的：要嘛整批刪掉，要嘛完全沒動，
不會出現「刪到一半」的狀態。

看紅色叉叉 ❌ 出現在哪一個步驟：

- 紅色出現在「**執行清除**」之前或當下 → 你的資料**完好無缺**，什麼都沒被刪。
- 紅色出現在「**補齊管理員與預設資料**」 → 資料**已經刪掉了**，但管理員還沒重建完。
  這時候還沒有人能登入。請到 Actions 按一次 **Deploy** 按鈕，
  它會把設定、管理員與範例會議室補回來。

點開紅色的步驟看最下面的訊息：

- 寫著「確認文字不正確」→ 重新執行一次，確認文字欄位輸入大寫的 `DELETE`。
- 寫著「缺少 Secret：某某名稱」→ 依照 [SETUP.md](SETUP.md) 的步驟四補上該 Secret。
  特別是 `ADMIN_EMAIL` 與 `ADMIN_INITIAL_PASSWORD`：
  少了它們，系統刪光帳號之後就沒辦法幫你重建管理員，所以會事先擋下來。
